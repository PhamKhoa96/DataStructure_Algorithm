/*

Overload
- Compile time polymorphism
- Xảy ra trong cùng 1 lớp  (các hàm cùng tên được khai báo trong cùng 1 class, nhưng chúng khác các dữ liệu input vào)
- Cho phép nhiều hơn 1 phương thức cùng tên nhưng khác nhau kiểu, số lượng và thứ tự tham số

Overriding
- Runtime polymorphism
- Xảy ra trong nhiều hơn 1 lớp trong mối quan hệ kế thừa (lớp con modify lại hàm từ lớp cha)
- Cho phép lớp con tái định nghĩa lại một phương thức của lớp cha theo cách riêng

*/